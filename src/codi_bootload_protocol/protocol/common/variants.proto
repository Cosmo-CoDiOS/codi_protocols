syntax = "proto3";

package protocol_common_variants;

// Enum defines the two (currently) different variants of CoDi.
enum CoDiVariant {
    STOCK = 0; // Stock CoDi enum value is an integer.
    CODIOS = 1; // CoDiOS enum value is an integer.
}
